---
const features = [
	{
		icon: 'grid_view',
		iconColor: 'text-secondary',
		title: 'Tilesets',
		description: 'Import and slice pixel art tilesets with grid-accurate controls built for fast 2D level design.',
	},
	{
		icon: 'layers',
		iconColor: 'text-primary',
		title: 'Layers',
		description: 'Build complex 2D maps with multi-layer editing, better visual organization, and faster iteration.',
	},
	{
		icon: 'architecture',
		iconColor: 'text-primary',
		title: 'Collision',
		description: 'Paint collision directly on the grid and export gameplay-ready metadata with your level layout.',
	},
	{
		icon: 'output',
		iconColor: 'text-primary',
		title: 'Exports',
		description: 'Export JSON and XML map data for Godot, Unity, and custom 2D engine pipelines without extra cleanup.',
	},
];
---

<section class="mx-auto w-full max-w-7xl px-6 py-16 lg:px-8" id="features">
	<div class="grid grid-cols-1 gap-4 md:grid-cols-2 xl:grid-cols-4">
		{features.map((feature, index) => (
			<article class={`reveal reveal-delay-${(index % 4) + 1} flex flex-col gap-4 rounded-xl border border-outline/35 bg-surface-container-low/70 p-8 backdrop-blur-2xl`}>
				<span class:list={['material-symbols-outlined text-[32px]', feature.iconColor]}>{feature.icon}</span>
				<h2 class="text-2xl font-medium tracking-[-0.03em] text-primary">{feature.title}</h2>
				<p class="text-base leading-7 text-on-surface-variant">{feature.description}</p>
			</article>
		))}
	</div>
</section>
